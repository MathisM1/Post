{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.type) }}
    {{ form_row(form.path) }}
    {{ form_row(form.alt) }}
    <button type="submit">Creer Media</button
{{ form_end(form) }}